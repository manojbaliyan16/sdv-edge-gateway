// main.cpp — entry point stub
// Real implementation wires all modules once can_reader, telemetry_publisher,
// command_handler, ota_agent, anomaly_detector are complete.

int main() {
    return 0;
}
